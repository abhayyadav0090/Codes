/*
Author: Abhay Yadav
Program: Basic of Unordered_Set
Date: 25/09/2026
*/

#include <bits/stdc++.h>
using namespace std;

void explainUSet(){
    unordered_set<int> st;
    // it also stores element uniquely like set
    // lower and upper bound functions does not works, 
    // rest all functions are same as above, it does not stores in any particular order
    // it has a better complexity than set in most cases, except some when collision happens
}

int main(){
    explainUSet();
    return 0;
}